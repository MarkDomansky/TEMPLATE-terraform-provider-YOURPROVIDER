// MANAGED FILE - do not edit in your fork.
// Discovery shim only: semantic-release finds this via cosmiconfig; the real
// config lives in .template/release/releaserc.cjs. Keep this a .cjs file - a
// stray .releaserc.yml/.yaml would shadow it (cosmiconfig search order).
module.exports = require('./.template/release/releaserc.cjs');
